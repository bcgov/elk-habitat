
calc_disturbance <- function(vri, depletions, change_detection) {
  # Prepare VRI
  # Calc disturbance year column
  # Choose polygon w maximum extent
  # Rasterize VRI + deps
  # Merge VRI, deps, cd
  # Return
}